# Dev dependencies (not needed for running the pipeline, only for development)
pytest>=7.4.0
pytest-cov>=4.1.0          # Coverage reports
black>=23.0.0               # Code formatter
isort>=5.12.0               # Import sorter
flake8>=6.0.0               # Linter
mypy>=1.5.0                 # Static type checker
